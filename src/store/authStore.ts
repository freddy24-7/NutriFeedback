// Auth state is managed by @stackframe/stack (StackProvider + useUser hook).
// Use useUser({ or: 'return-null' }) from '@stackframe/stack' in components.
// Use stackApp.getUser() from '@/lib/auth/client' in async non-React contexts.
//
// This file is intentionally empty — kept as a placeholder so existing imports
// do not break during the transition. Remove once all references are updated.

export {};
